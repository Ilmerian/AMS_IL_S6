import { supabase } from '../lib/supabaseClient';import { Pitch } from '../models/Pitch';
export const PitchRepository={async list(){const {data,error}=await supabase.from('pitches').select('*').order('created_at',{ascending:false});if(error) throw error;return data.map(Pitch.fromRow);},async create(payload){const {data,error}=await supabase.from('pitches').insert(payload).select().single();if(error) throw error;return Pitch.fromRow(data);}};
